import 'package:flutter/cupertino.dart';

class PayDues extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('paydues here'),
    );
  }
}
